# artifacts

The trained FP32 checkpoint lives here as `fp32_model.pt`, generated by:
```bash
python -m model.train --epochs 5
```

This trains the MLP for 5 epochs. 

The benchmark, tests and `model.inspect` all load this file.
